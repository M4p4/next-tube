/*export const wordExists = async (wordSearch: string) => {
  const spinner = await Spinner.findOne({ words: wordSearch });
  if (spinner) return true;
  return false;
};*/

export const test = () => {};
